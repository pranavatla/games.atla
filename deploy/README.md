# Deploy Output

This folder is reserved for exported web builds.

Target layout after export:

- `deploy/web/index.html`
- `deploy/web/*.wasm`
- `deploy/web/*.pck`
- `deploy/web/*.js`
- `deploy/web/*.png`

The `games.atla.in` host should point at this folder once the Godot export is generated.

